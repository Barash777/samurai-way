import {AllActionsType, SidebarType} from '../Types';

const initialState: SidebarType = {}

const sidebarReducer = (state: SidebarType = initialState, action: AllActionsType) => {

    return state
}

export default sidebarReducer;